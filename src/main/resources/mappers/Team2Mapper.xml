<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.websocket.socket.mapper.Team2Mapper">

    <select id="findAll" resultType="com.websocket.socket.entity.Team2Entity">
		SELECT
			*
		FROM chatting_supervisor
		ORDER BY id DESC
	</select>

	<insert id="createData">
		INSERT INTO chatting_supervisor(chat_time, username, chatting_content, chat_ip, chat_check)
		VALUES (#{chatTime}, #{username}, #{chattingContent}, #{ip}, #{chatCheck})
	</insert>

	<update id="updateData">
		UPDATE chatting_supervisor
		SET username = #{username}, chat_check = #{chatCheck}
		WHERE id=#{id}
	</update>

	<delete id="deleteData">
		DELETE FROM chatting_supervisor
		WHERE id=#{id}
	</delete>

</mapper>
